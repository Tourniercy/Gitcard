export interface CardOptions {
  theme: string;
  hide: string[];
  showIcons: boolean;
  hideBorder: boolean;
  hideTitle: boolean;
  bgColor?: string;
  titleColor?: string;
  textColor?: string;
  iconColor?: string;
  borderColor?: string;
  cacheSeconds: number;
  locale: string;
}
